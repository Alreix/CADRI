# Files/directories that must never be copied into the frontend Docker image:
# node_modules, the dev build output, .env files with real secrets, and
# anything else that only makes sense on a developer machine.
