# Template listing every environment variable required in production.
# Copy this to a real .env on the server and fill in actual values there —
# never commit real secrets. Should list at minimum: SECRET_KEY,
# JWT_SECRET_KEY, DATABASE_URL, FRONTEND_URL, REFRESH_COOKIE_SECURE=true,
# and production mail server settings (replacing the dev Mailpit config).
