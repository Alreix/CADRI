# Reverse proxy configuration for production.
# Expected to: terminate TLS (real certificate + key, or a Let's Encrypt
# integration), redirect plain HTTP to HTTPS, route the API path prefix to
# the backend service and everything else to the frontend's static files,
# and set standard security headers (HSTS, X-Content-Type-Options, etc.).
